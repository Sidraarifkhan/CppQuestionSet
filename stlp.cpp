#include<iostream>
#include<vector>
using namespace std;

int main(){
    //standard template library 
    // containers: obj that stores data----->
    // sequence(linear fashion)ex vector list deqeue:::: vector: rA fast, insertion del slow
    //,associative(direct access//tree)ex set multiset map fast searching & derived(real world modeling)ex stack que priority queue
    // algo: procedure to process data
    // itertors: obj which points to the element of a container
    vector<int> vect1;


  return 0;

}